package com.ryangehring.cake.solns;

import java.util.LinkedList;
import java.util.List;

/**
 * Created by ryan on 8/28/16.
 */
public class P22 {
    public void deleteNode() {
        // delete a node in n time --- iterate thru the list with a 'head'
        // and 'trail' cursor. when you reach the element to delete, make
        // 'trail'.next point to head.next.

        // delete a node in constant time (assumes you have a reference to the node)
        // just copy 'next' node to the node to be deleted.
    }
}
